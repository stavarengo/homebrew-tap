name: CI

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 7 * * 1"

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Homebrew
        if: matrix.os == 'ubuntu-latest'
        uses: Homebrew/actions/setup-homebrew@master

      - name: Audit formula
        run: brew audit --strict --online Formula/mobt.rb

      - name: Install formula from source
        run: brew install --build-from-source ./Formula/mobt.rb

      - name: Smoke test mobt CLI
        run: |
          set -euo pipefail
          mobt --help > mobt-help.txt
          grep -q "Usage:" mobt-help.txt
          mob-tool --help > mob-tool-help.txt
          grep -q "Usage:" mob-tool-help.txt
